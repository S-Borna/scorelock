"use client";

/**
 * Placeholder provider — Sentry removed.
 * Kept as a passthrough so layout.tsx doesn't need refactoring.
 */
export function SentryProvider({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
